#include <stdio.h>
#include <math.h>
#include <string.h>

int main() {
    //1. Raiz quadrada
    double raizQuadrada = sqrt(9);
    printf("Raiz quadrada de 9: %.2f\n", raizQuadrada);
    //2. Potência
    double potencia = pow(3, 3);
    printf("Potencia de 3: %.2f\n", potencia);
    //3.
    double seno = sin(53);
    printf("Seno de 53: %.2f\n", seno);

    double coseno= cos(99);
    printf("Coseno de 99: %.2f\n", coseno);



    printf("Fim do Programa.\n");
    return 0;
}